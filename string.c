#include<stdio.h>
#include<string.h>
int main()
{
    char str[20],rev[20];
    int length=0,i,j;
    printf("enter any string");
    gets(str);
    for(i=0;i!='\0',i++)
    {
        length++;
    }
    for(i=0;i<=length;i++)
    {
        for("j=")
    }

    
}